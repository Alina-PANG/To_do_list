
<%= form_for(:list, url: user_lists_path) do |f| %>

<% end %>
<% if @list.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@list.errors.count, "error") %> prohibited
        this list from being saved:
      </h2>
      <ul>
        <% @list.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <br><br>
  <div class="form-group">
      <%= f.label :list_name, "List Name", class: "col-md-2 control-label" %>
      <div class="col-md-8">
        <%= f.text_field :list_name, :placeholder => "List Name", class: "form-control" %>
      </div>
  </div>
<br><br><br>
  <div class="form-group">
    <div class="col-md-2"></div>
    <% if true %>
    <div class="col-md-8">
      <%= f.submit "Create New List", class: "btn btn-default btn-primary col-md-8"  %>
    </div>
    <% else %>
    <div class="col-md-8">
      <%= f.submit "Submit", class: "btn btn-default btn-primary col-md-8"  %>
    </div>
    <% end %>
  </div>
<% end %>
